REM This is a batch file to run the CLI version of ERQL while not needing to have a Python environment set up. It assumes that Python 3 is installed and added to the system PATH.

@echo off
python3 cli.py
pause