1. Run these in powershell:

New-Item -ItemType Directory -Path (Split-Path $PROFILE) -Force
New-Item -ItemType File -Path $PROFILE -Force

2. Create and open PROFILE folder:

notepad $PROFILE

3. Link powershell command to python script:

function [name] {
    python "[script filepath]" @args
}